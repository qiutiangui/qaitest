---
name: 功能点详情弹窗优化
overview: 将功能点列表页的"查看详情"从路由跳转改为弹窗形式，风格与用例详情弹窗保持一致。
todos:
  - id: add-detail-state
    content: 在 RequirementList.vue 中添加详情弹窗状态变量 showDetail 和 detailRequirement
    status: completed
  - id: add-format-date
    content: 添加 formatDate 格式化日期函数
    status: completed
  - id: modify-handle-view
    content: 修改 handleView 函数，改为显示弹窗而非路由跳转
    status: completed
  - id: add-detail-modal
    content: 在模板末尾添加详情弹窗 HTML，风格参考用例详情弹窗
    status: completed
---

## 用户需求

将功能点管理页面的详情展示从路由跳转改为弹窗形式，风格与用例详情弹窗保持一致。

## 核心功能

- 点击功能点名称或查看按钮时，显示弹窗详情而非跳转页面
- 弹窗风格与用例详情弹窗统一：背景遮罩、内容容器、标题栏、字段布局等
- 展示功能点的完整信息：名称、项目、版本、模块、类别、优先级、描述、验收标准、关键词、时间等

## 涉及页面

- `frontend/src/views/RequirementList.vue` - 功能点列表页，需要添加详情弹窗
- 参考页面：`frontend/src/views/TestCaseList.vue` (第367-458行) - 用例详情弹窗样式

## 修改方案

### 文件：`frontend/src/views/RequirementList.vue`

#### 1. 添加状态变量（在第45行后添加）

```typescript
// 详情弹窗
const showDetail = ref(false)
const detailRequirement = ref<Requirement | null>(null)
```

#### 2. 添加格式化日期函数（在 `handleSave` 函数后添加）

```typescript
// 格式化日期
function formatDate(date: string | null): string {
  if (!date) return '-'
  return new Date(date).toLocaleString('zh-CN')
}
```

#### 3. 修改 `handleView` 函数（约第204行）

将原来的路由跳转改为显示弹窗：

```typescript
// 查看详情 - 改为弹窗形式
function handleView(req: Requirement) {
  detailRequirement.value = req
  showDetail.value = true
}
```

#### 4. 在模板末尾添加详情弹窗（在新建/编辑对话框后面添加）

```html
<!-- 详情弹窗 -->
<div
  v-if="showDetail && detailRequirement"
  class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50"
  @click.self="showDetail = false"
>
  <div class="bg-white rounded-lg shadow-xl max-w-3xl w-full max-h-[90vh] overflow-y-auto">
    <div class="p-6">
      <!-- 弹窗标题 -->
      <div class="flex items-center justify-between mb-4">
        <h2 class="text-xl font-semibold text-text-primary">功能点详情</h2>
        <button
          class="text-text-secondary hover:text-text-primary transition-colors"
          @click="showDetail = false"
        >
          ✕
        </button>
      </div>

      <!-- 基本信息 -->
      <div class="space-y-4">
        <div>
          <label class="text-sm text-text-secondary">功能点名称</label>
          <p class="text-text-primary font-medium">{{ detailRequirement.name }}</p>
        </div>

        <div class="grid grid-cols-3 gap-4">
          <div>
            <label class="text-sm text-text-secondary">所属项目</label>
            <p class="text-text-primary">{{ getProjectName(detailRequirement.project_id) }}</p>
          </div>
          <div>
            <label class="text-sm text-text-secondary">所属版本</label>
            <p class="text-text-primary">{{ getVersionName(detailRequirement.version_id) }}</p>
          </div>
          <div>
            <label class="text-sm text-text-secondary">所属模块</label>
            <p class="text-text-primary">{{ detailRequirement.module || '-' }}</p>
          </div>
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="text-sm text-text-secondary">类别</label>
            <p class="text-text-primary">{{ detailRequirement.category || '-' }}</p>
          </div>
          <div>
            <label class="text-sm text-text-secondary">优先级</label>
            <p>
              <span 
                v-if="detailRequirement.priority" 
                :class="[
                  'px-2 py-0.5 rounded text-xs',
                  detailRequirement.priority === '高' ? 'bg-red-100 text-red-700' :
                  detailRequirement.priority === '中' ? 'bg-orange-100 text-orange-700' :
                  'bg-green-100 text-green-700'
                ]"
              >
                {{ detailRequirement.priority }}
              </span>
              <span v-else class="text-text-primary">-</span>
            </p>
          </div>
        </div>

        <div>
          <label class="text-sm text-text-secondary">功能点描述</label>
          <p class="text-text-primary whitespace-pre-wrap">
            {{ detailRequirement.description || '无' }}
          </p>
        </div>

        <div>
          <label class="text-sm text-text-secondary">验收标准</label>
          <p class="text-text-primary whitespace-pre-wrap">
            {{ detailRequirement.acceptance_criteria || '无' }}
          </p>
        </div>

        <div>
          <label class="text-sm text-text-secondary">关键词</label>
          <p class="text-text-primary">
            {{ detailRequirement.keywords || '无' }}
          </p>
        </div>

        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="text-sm text-text-secondary">创建时间</label>
            <p class="text-text-primary">{{ formatDate(detailRequirement.created_at) }}</p>
          </div>
          <div>
            <label class="text-sm text-text-secondary">更新时间</label>
            <p class="text-text-primary">{{ formatDate(detailRequirement.updated_at) }}</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

## 弹窗风格对比

| 元素 | 用例详情弹窗 | 功能点详情弹窗（新） |
| --- | --- | --- |
| 背景遮罩 | `fixed inset-0 bg-black bg-opacity-50` | 相同 |
| 内容容器 | `bg-white rounded-lg shadow-xl max-w-3xl w-full max-h-[90vh]` | 相同 |
| 标题栏 | `flex items-center justify-between mb-4` | 相同 |
| 关闭按钮 | `✕` 文字按钮 | 相同 |
| 字段标签 | `text-sm text-text-secondary` | 相同 |
| 字段值 | `text-text-primary` 或 `text-text-primary font-medium` | 相同 |
| 内容区域 | `space-y-4` | 相同 |